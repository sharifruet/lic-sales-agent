# Web Frontend Placeholder

The web frontend will live here (Next.js/React or similar).  
During the backend migration, this directory remains optional and can be ignored if you prefer API-only workflows.

